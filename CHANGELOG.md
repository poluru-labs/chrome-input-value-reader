# Changelog

## Unreleased

- Inspector popup: ink masthead, structured result card, example selector chips
- Load unpacked from the repository root (`manifest.json` at the top level)

## 1.0.0 — 2026-09-02

Initial public release.

- Manifest V3 popup that reads input, textarea, and select values by CSS selector
- Highlights the matched element on the page
- Password field values are not displayed
- Runs on the active tab after a user click (`activeTab` + `scripting`)
- MIT license
